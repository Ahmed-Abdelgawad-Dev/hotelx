services:
  docs:
    image: hotelx_local_docs
    container_name: hotelx_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./hotelx:/app/hotelx:z
    ports:
      - '9000:9000'
    command: /start-docs
